$defs:
  query_sample_collection:
    type: object
    properties:
      allowed_comment_keys:
        type: array
        items:
          type: string
      max_rows_per_query:
        type: integer
        x-customType: uint64
  statement_events_config:
    type: object
    properties:
      digest_text_limit:
        type: integer
      limit:
        type: integer
      time_limit:
        type: string
        format: duration
  top_query_collection:
    type: object
    properties:
      allowed_comment_keys:
        type: array
        items:
          type: string
      collection_interval:
        type: string
        format: duration
      lookback_time:
        type: integer
        x-customType: uint64
      max_query_sample_count:
        type: integer
        x-customType: uint64
      query_plan_cache_size:
        type: integer
      query_plan_cache_ttl:
        type: string
        format: duration
      top_query_count:
        type: integer
        x-customType: uint64
type: object
properties:
  allow_native_passwords:
    type: boolean
  database:
    type: string
  explain_mode:
    description: 'ExplainMode selects how execution plans are retrieved: "inline" (default, direct EXPLAIN on the monitoring connection) or "procedure" (CALL the <schema>.explain_statement SQL SECURITY DEFINER procedure, so write statements can be explained without granting DML to the monitoring user).'
    type: string
  password:
    $ref: go.opentelemetry.io/collector/config/configopaque.string
  query_sample_collection:
    $ref: query_sample_collection
  statement_events:
    $ref: statement_events_config
  tls:
    $ref: go.opentelemetry.io/collector/config/configtls.client_config
  top_query_collection:
    $ref: top_query_collection
  username:
    type: string
allOf:
  - $ref: go.opentelemetry.io/collector/scraper/scraperhelper.controller_config
  - $ref: go.opentelemetry.io/collector/config/confignet.addr_config
  - $ref: ./internal/metadata.metrics_builder_config
  - $ref: ./internal/metadata.logs_builder_config
