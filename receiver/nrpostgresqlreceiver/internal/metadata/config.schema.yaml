# Code generated by mdatagen. DO NOT EDIT.
$defs:
  attribute_bg_buffer_source:
    description: AttributeBgBufferSource specifies the value bg_buffer_source attribute.
    type: integer
  attribute_bg_checkpoint_type:
    description: AttributeBgCheckpointType specifies the value bg_checkpoint_type attribute.
    type: integer
  attribute_bg_duration_type:
    description: AttributeBgDurationType specifies the value bg_duration_type attribute.
    type: integer
  attribute_db_system_name:
    description: AttributeDbSystemName specifies the value db.system.name attribute.
    type: integer
  attribute_operation:
    description: AttributeOperation specifies the value operation attribute.
    type: integer
  attribute_postgresql_conflict_type:
    description: AttributePostgresqlConflictType specifies the value postgresql.conflict.type attribute.
    type: integer
  attribute_postgresql_distance_function_name:
    description: AttributePostgresqlDistanceFunctionName specifies the value postgresql.distance.function.name attribute.
    type: integer
  attribute_source:
    description: AttributeSource specifies the value source attribute.
    type: integer
  attribute_state:
    description: AttributeState specifies the value state attribute.
    type: integer
  attribute_wal_operation_lag:
    description: AttributeWalOperationLag specifies the value wal_operation_lag attribute.
    type: integer
  event_config:
    description: EventConfig provides common config for a particular event.
    type: object
    properties:
      enabled:
        type: boolean
  events_config:
    description: EventsConfig provides config for nrpostgresql events.
    type: object
    properties:
      db.server.query_sample:
        $ref: event_config
      db.server.top_query:
        $ref: event_config
  logs_builder_config:
    description: LogsBuilderConfig is a configuration for nrpostgresql logs builder.
    type: object
    properties:
      events:
        $ref: events_config
      resource_attributes:
        $ref: resource_attributes_config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for nrpostgresql metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
  metrics_config:
    description: MetricsConfig provides config for nrpostgresql metrics.
    type: object
    properties:
      postgresql.backends:
        $ref: postgresql_backends_metric_config
      postgresql.bgwriter.buffers.allocated:
        $ref: postgresql_bgwriter_buffers_allocated_metric_config
      postgresql.bgwriter.buffers.writes:
        $ref: postgresql_bgwriter_buffers_writes_metric_config
      postgresql.bgwriter.checkpoint.count:
        $ref: postgresql_bgwriter_checkpoint_count_metric_config
      postgresql.bgwriter.duration:
        $ref: postgresql_bgwriter_duration_metric_config
      postgresql.bgwriter.maxwritten:
        $ref: postgresql_bgwriter_maxwritten_metric_config
      postgresql.blks_hit:
        $ref: postgresql_blks_hit_metric_config
      postgresql.blks_read:
        $ref: postgresql_blks_read_metric_config
      postgresql.blocks_read:
        $ref: postgresql_blocks_read_metric_config
      postgresql.commits:
        $ref: postgresql_commits_metric_config
      postgresql.connection.max:
        $ref: postgresql_connection_max_metric_config
      postgresql.database.count:
        $ref: postgresql_database_count_metric_config
      postgresql.database.locks:
        $ref: postgresql_database_locks_metric_config
      postgresql.db_size:
        $ref: postgresql_db_size_metric_config
      postgresql.deadlocks:
        $ref: postgresql_deadlocks_metric_config
      postgresql.function.calls:
        $ref: postgresql_function_calls_metric_config
      postgresql.index.scans:
        $ref: postgresql_index_scans_metric_config
      postgresql.index.size:
        $ref: postgresql_index_size_metric_config
      postgresql.operations:
        $ref: postgresql_operations_metric_config
      postgresql.query.conflicts:
        $ref: postgresql_query_conflicts_metric_config
      postgresql.query.execution.time:
        $ref: postgresql_query_execution_time_metric_config
      postgresql.replication.data_delay:
        $ref: postgresql_replication_data_delay_metric_config
      postgresql.rollbacks:
        $ref: postgresql_rollbacks_metric_config
      postgresql.rows:
        $ref: postgresql_rows_metric_config
      postgresql.sequential_scans:
        $ref: postgresql_sequential_scans_metric_config
      postgresql.table.count:
        $ref: postgresql_table_count_metric_config
      postgresql.table.size:
        $ref: postgresql_table_size_metric_config
      postgresql.table.vacuum.count:
        $ref: postgresql_table_vacuum_count_metric_config
      postgresql.temp.io:
        $ref: postgresql_temp_io_metric_config
      postgresql.temp_files:
        $ref: postgresql_temp_files_metric_config
      postgresql.tup_deleted:
        $ref: postgresql_tup_deleted_metric_config
      postgresql.tup_fetched:
        $ref: postgresql_tup_fetched_metric_config
      postgresql.tup_inserted:
        $ref: postgresql_tup_inserted_metric_config
      postgresql.tup_returned:
        $ref: postgresql_tup_returned_metric_config
      postgresql.tup_updated:
        $ref: postgresql_tup_updated_metric_config
      postgresql.vector.insert.duration:
        $ref: postgresql_vector_insert_duration_metric_config
      postgresql.vector.insert.rows:
        $ref: postgresql_vector_insert_rows_metric_config
      postgresql.vector.search.calls:
        $ref: postgresql_vector_search_calls_metric_config
      postgresql.vector.search.duration:
        $ref: postgresql_vector_search_duration_metric_config
      postgresql.vector.search.rows_returned:
        $ref: postgresql_vector_search_rows_returned_metric_config
      postgresql.wal.age:
        $ref: postgresql_wal_age_metric_config
      postgresql.wal.delay:
        $ref: postgresql_wal_delay_metric_config
      postgresql.wal.lag:
        $ref: postgresql_wal_lag_metric_config
  postgresql_backends_metric_attribute_key:
    description: PostgresqlBackendsMetricAttributeKey specifies the key of an attribute for the postgresql.backends metric.
    type: string
  postgresql_backends_metric_config:
    description: PostgresqlBackendsMetricConfig provides config for the postgresql.backends metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_backends_metric_attribute_key
      enabled:
        type: boolean
  postgresql_bgwriter_buffers_allocated_metric_config:
    description: PostgresqlBgwriterBuffersAllocatedMetricConfig provides config for the postgresql.bgwriter.buffers.allocated metric.
    type: object
    properties:
      enabled:
        type: boolean
  postgresql_bgwriter_buffers_writes_metric_attribute_key:
    description: PostgresqlBgwriterBuffersWritesMetricAttributeKey specifies the key of an attribute for the postgresql.bgwriter.buffers.writes metric.
    type: string
  postgresql_bgwriter_buffers_writes_metric_config:
    description: PostgresqlBgwriterBuffersWritesMetricConfig provides config for the postgresql.bgwriter.buffers.writes metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_bgwriter_buffers_writes_metric_attribute_key
      enabled:
        type: boolean
  postgresql_bgwriter_checkpoint_count_metric_attribute_key:
    description: PostgresqlBgwriterCheckpointCountMetricAttributeKey specifies the key of an attribute for the postgresql.bgwriter.checkpoint.count metric.
    type: string
  postgresql_bgwriter_checkpoint_count_metric_config:
    description: PostgresqlBgwriterCheckpointCountMetricConfig provides config for the postgresql.bgwriter.checkpoint.count metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_bgwriter_checkpoint_count_metric_attribute_key
      enabled:
        type: boolean
  postgresql_bgwriter_duration_metric_attribute_key:
    description: PostgresqlBgwriterDurationMetricAttributeKey specifies the key of an attribute for the postgresql.bgwriter.duration metric.
    type: string
  postgresql_bgwriter_duration_metric_config:
    description: PostgresqlBgwriterDurationMetricConfig provides config for the postgresql.bgwriter.duration metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_bgwriter_duration_metric_attribute_key
      enabled:
        type: boolean
  postgresql_bgwriter_maxwritten_metric_config:
    description: PostgresqlBgwriterMaxwrittenMetricConfig provides config for the postgresql.bgwriter.maxwritten metric.
    type: object
    properties:
      enabled:
        type: boolean
  postgresql_blks_hit_metric_attribute_key:
    description: PostgresqlBlksHitMetricAttributeKey specifies the key of an attribute for the postgresql.blks_hit metric.
    type: string
  postgresql_blks_hit_metric_config:
    description: PostgresqlBlksHitMetricConfig provides config for the postgresql.blks_hit metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_blks_hit_metric_attribute_key
      enabled:
        type: boolean
  postgresql_blks_read_metric_attribute_key:
    description: PostgresqlBlksReadMetricAttributeKey specifies the key of an attribute for the postgresql.blks_read metric.
    type: string
  postgresql_blks_read_metric_config:
    description: PostgresqlBlksReadMetricConfig provides config for the postgresql.blks_read metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_blks_read_metric_attribute_key
      enabled:
        type: boolean
  postgresql_blocks_read_metric_attribute_key:
    description: PostgresqlBlocksReadMetricAttributeKey specifies the key of an attribute for the postgresql.blocks_read metric.
    type: string
  postgresql_blocks_read_metric_config:
    description: PostgresqlBlocksReadMetricConfig provides config for the postgresql.blocks_read metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_blocks_read_metric_attribute_key
      enabled:
        type: boolean
  postgresql_commits_metric_attribute_key:
    description: PostgresqlCommitsMetricAttributeKey specifies the key of an attribute for the postgresql.commits metric.
    type: string
  postgresql_commits_metric_config:
    description: PostgresqlCommitsMetricConfig provides config for the postgresql.commits metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_commits_metric_attribute_key
      enabled:
        type: boolean
  postgresql_connection_max_metric_config:
    description: PostgresqlConnectionMaxMetricConfig provides config for the postgresql.connection.max metric.
    type: object
    properties:
      enabled:
        type: boolean
  postgresql_database_count_metric_config:
    description: PostgresqlDatabaseCountMetricConfig provides config for the postgresql.database.count metric.
    type: object
    properties:
      enabled:
        type: boolean
  postgresql_database_locks_metric_attribute_key:
    description: PostgresqlDatabaseLocksMetricAttributeKey specifies the key of an attribute for the postgresql.database.locks metric.
    type: string
  postgresql_database_locks_metric_config:
    description: PostgresqlDatabaseLocksMetricConfig provides config for the postgresql.database.locks metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_database_locks_metric_attribute_key
      enabled:
        type: boolean
  postgresql_database_name_resource_attribute_config:
    description: PostgresqlDatabaseNameResourceAttributeConfig provides config for the postgresql.database.name resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
  postgresql_db_size_metric_attribute_key:
    description: PostgresqlDbSizeMetricAttributeKey specifies the key of an attribute for the postgresql.db_size metric.
    type: string
  postgresql_db_size_metric_config:
    description: PostgresqlDbSizeMetricConfig provides config for the postgresql.db_size metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_db_size_metric_attribute_key
      enabled:
        type: boolean
  postgresql_deadlocks_metric_attribute_key:
    description: PostgresqlDeadlocksMetricAttributeKey specifies the key of an attribute for the postgresql.deadlocks metric.
    type: string
  postgresql_deadlocks_metric_config:
    description: PostgresqlDeadlocksMetricConfig provides config for the postgresql.deadlocks metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_deadlocks_metric_attribute_key
      enabled:
        type: boolean
  postgresql_function_calls_metric_attribute_key:
    description: PostgresqlFunctionCallsMetricAttributeKey specifies the key of an attribute for the postgresql.function.calls metric.
    type: string
  postgresql_function_calls_metric_config:
    description: PostgresqlFunctionCallsMetricConfig provides config for the postgresql.function.calls metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_function_calls_metric_attribute_key
      enabled:
        type: boolean
  postgresql_index_name_resource_attribute_config:
    description: PostgresqlIndexNameResourceAttributeConfig provides config for the postgresql.index.name resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
  postgresql_index_scans_metric_attribute_key:
    description: PostgresqlIndexScansMetricAttributeKey specifies the key of an attribute for the postgresql.index.scans metric.
    type: string
  postgresql_index_scans_metric_config:
    description: PostgresqlIndexScansMetricConfig provides config for the postgresql.index.scans metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_index_scans_metric_attribute_key
      enabled:
        type: boolean
  postgresql_index_size_metric_attribute_key:
    description: PostgresqlIndexSizeMetricAttributeKey specifies the key of an attribute for the postgresql.index.size metric.
    type: string
  postgresql_index_size_metric_config:
    description: PostgresqlIndexSizeMetricConfig provides config for the postgresql.index.size metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_index_size_metric_attribute_key
      enabled:
        type: boolean
  postgresql_operations_metric_attribute_key:
    description: PostgresqlOperationsMetricAttributeKey specifies the key of an attribute for the postgresql.operations metric.
    type: string
  postgresql_operations_metric_config:
    description: PostgresqlOperationsMetricConfig provides config for the postgresql.operations metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_operations_metric_attribute_key
      enabled:
        type: boolean
  postgresql_query_conflicts_metric_attribute_key:
    description: PostgresqlQueryConflictsMetricAttributeKey specifies the key of an attribute for the postgresql.query.conflicts metric.
    type: string
  postgresql_query_conflicts_metric_config:
    description: PostgresqlQueryConflictsMetricConfig provides config for the postgresql.query.conflicts metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_query_conflicts_metric_attribute_key
      enabled:
        type: boolean
  postgresql_query_execution_time_metric_attribute_key:
    description: PostgresqlQueryExecutionTimeMetricAttributeKey specifies the key of an attribute for the postgresql.query.execution.time metric.
    type: string
  postgresql_query_execution_time_metric_config:
    description: PostgresqlQueryExecutionTimeMetricConfig provides config for the postgresql.query.execution.time metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_query_execution_time_metric_attribute_key
      enabled:
        type: boolean
  postgresql_replication_data_delay_metric_attribute_key:
    description: PostgresqlReplicationDataDelayMetricAttributeKey specifies the key of an attribute for the postgresql.replication.data_delay metric.
    type: string
  postgresql_replication_data_delay_metric_config:
    description: PostgresqlReplicationDataDelayMetricConfig provides config for the postgresql.replication.data_delay metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_replication_data_delay_metric_attribute_key
      enabled:
        type: boolean
  postgresql_rollbacks_metric_attribute_key:
    description: PostgresqlRollbacksMetricAttributeKey specifies the key of an attribute for the postgresql.rollbacks metric.
    type: string
  postgresql_rollbacks_metric_config:
    description: PostgresqlRollbacksMetricConfig provides config for the postgresql.rollbacks metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_rollbacks_metric_attribute_key
      enabled:
        type: boolean
  postgresql_rows_metric_attribute_key:
    description: PostgresqlRowsMetricAttributeKey specifies the key of an attribute for the postgresql.rows metric.
    type: string
  postgresql_rows_metric_config:
    description: PostgresqlRowsMetricConfig provides config for the postgresql.rows metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_rows_metric_attribute_key
      enabled:
        type: boolean
  postgresql_schema_name_resource_attribute_config:
    description: PostgresqlSchemaNameResourceAttributeConfig provides config for the postgresql.schema.name resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
  postgresql_sequential_scans_metric_attribute_key:
    description: PostgresqlSequentialScansMetricAttributeKey specifies the key of an attribute for the postgresql.sequential_scans metric.
    type: string
  postgresql_sequential_scans_metric_config:
    description: PostgresqlSequentialScansMetricConfig provides config for the postgresql.sequential_scans metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_sequential_scans_metric_attribute_key
      enabled:
        type: boolean
  postgresql_table_count_metric_attribute_key:
    description: PostgresqlTableCountMetricAttributeKey specifies the key of an attribute for the postgresql.table.count metric.
    type: string
  postgresql_table_count_metric_config:
    description: PostgresqlTableCountMetricConfig provides config for the postgresql.table.count metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_table_count_metric_attribute_key
      enabled:
        type: boolean
  postgresql_table_name_resource_attribute_config:
    description: PostgresqlTableNameResourceAttributeConfig provides config for the postgresql.table.name resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
  postgresql_table_size_metric_attribute_key:
    description: PostgresqlTableSizeMetricAttributeKey specifies the key of an attribute for the postgresql.table.size metric.
    type: string
  postgresql_table_size_metric_config:
    description: PostgresqlTableSizeMetricConfig provides config for the postgresql.table.size metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_table_size_metric_attribute_key
      enabled:
        type: boolean
  postgresql_table_vacuum_count_metric_attribute_key:
    description: PostgresqlTableVacuumCountMetricAttributeKey specifies the key of an attribute for the postgresql.table.vacuum.count metric.
    type: string
  postgresql_table_vacuum_count_metric_config:
    description: PostgresqlTableVacuumCountMetricConfig provides config for the postgresql.table.vacuum.count metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_table_vacuum_count_metric_attribute_key
      enabled:
        type: boolean
  postgresql_temp_files_metric_attribute_key:
    description: PostgresqlTempFilesMetricAttributeKey specifies the key of an attribute for the postgresql.temp_files metric.
    type: string
  postgresql_temp_files_metric_config:
    description: PostgresqlTempFilesMetricConfig provides config for the postgresql.temp_files metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_temp_files_metric_attribute_key
      enabled:
        type: boolean
  postgresql_temp_io_metric_attribute_key:
    description: PostgresqlTempIoMetricAttributeKey specifies the key of an attribute for the postgresql.temp.io metric.
    type: string
  postgresql_temp_io_metric_config:
    description: PostgresqlTempIoMetricConfig provides config for the postgresql.temp.io metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_temp_io_metric_attribute_key
      enabled:
        type: boolean
  postgresql_tup_deleted_metric_attribute_key:
    description: PostgresqlTupDeletedMetricAttributeKey specifies the key of an attribute for the postgresql.tup_deleted metric.
    type: string
  postgresql_tup_deleted_metric_config:
    description: PostgresqlTupDeletedMetricConfig provides config for the postgresql.tup_deleted metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_tup_deleted_metric_attribute_key
      enabled:
        type: boolean
  postgresql_tup_fetched_metric_attribute_key:
    description: PostgresqlTupFetchedMetricAttributeKey specifies the key of an attribute for the postgresql.tup_fetched metric.
    type: string
  postgresql_tup_fetched_metric_config:
    description: PostgresqlTupFetchedMetricConfig provides config for the postgresql.tup_fetched metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_tup_fetched_metric_attribute_key
      enabled:
        type: boolean
  postgresql_tup_inserted_metric_attribute_key:
    description: PostgresqlTupInsertedMetricAttributeKey specifies the key of an attribute for the postgresql.tup_inserted metric.
    type: string
  postgresql_tup_inserted_metric_config:
    description: PostgresqlTupInsertedMetricConfig provides config for the postgresql.tup_inserted metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_tup_inserted_metric_attribute_key
      enabled:
        type: boolean
  postgresql_tup_returned_metric_attribute_key:
    description: PostgresqlTupReturnedMetricAttributeKey specifies the key of an attribute for the postgresql.tup_returned metric.
    type: string
  postgresql_tup_returned_metric_config:
    description: PostgresqlTupReturnedMetricConfig provides config for the postgresql.tup_returned metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_tup_returned_metric_attribute_key
      enabled:
        type: boolean
  postgresql_tup_updated_metric_attribute_key:
    description: PostgresqlTupUpdatedMetricAttributeKey specifies the key of an attribute for the postgresql.tup_updated metric.
    type: string
  postgresql_tup_updated_metric_config:
    description: PostgresqlTupUpdatedMetricConfig provides config for the postgresql.tup_updated metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_tup_updated_metric_attribute_key
      enabled:
        type: boolean
  postgresql_vector_insert_duration_metric_attribute_key:
    description: PostgresqlVectorInsertDurationMetricAttributeKey specifies the key of an attribute for the postgresql.vector.insert.duration metric.
    type: string
  postgresql_vector_insert_duration_metric_config:
    description: PostgresqlVectorInsertDurationMetricConfig provides config for the postgresql.vector.insert.duration metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_vector_insert_duration_metric_attribute_key
      enabled:
        type: boolean
  postgresql_vector_insert_rows_metric_attribute_key:
    description: PostgresqlVectorInsertRowsMetricAttributeKey specifies the key of an attribute for the postgresql.vector.insert.rows metric.
    type: string
  postgresql_vector_insert_rows_metric_config:
    description: PostgresqlVectorInsertRowsMetricConfig provides config for the postgresql.vector.insert.rows metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_vector_insert_rows_metric_attribute_key
      enabled:
        type: boolean
  postgresql_vector_search_calls_metric_attribute_key:
    description: PostgresqlVectorSearchCallsMetricAttributeKey specifies the key of an attribute for the postgresql.vector.search.calls metric.
    type: string
  postgresql_vector_search_calls_metric_config:
    description: PostgresqlVectorSearchCallsMetricConfig provides config for the postgresql.vector.search.calls metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_vector_search_calls_metric_attribute_key
      enabled:
        type: boolean
  postgresql_vector_search_duration_metric_attribute_key:
    description: PostgresqlVectorSearchDurationMetricAttributeKey specifies the key of an attribute for the postgresql.vector.search.duration metric.
    type: string
  postgresql_vector_search_duration_metric_config:
    description: PostgresqlVectorSearchDurationMetricConfig provides config for the postgresql.vector.search.duration metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_vector_search_duration_metric_attribute_key
      enabled:
        type: boolean
  postgresql_vector_search_rows_returned_metric_attribute_key:
    description: PostgresqlVectorSearchRowsReturnedMetricAttributeKey specifies the key of an attribute for the postgresql.vector.search.rows_returned metric.
    type: string
  postgresql_vector_search_rows_returned_metric_config:
    description: PostgresqlVectorSearchRowsReturnedMetricConfig provides config for the postgresql.vector.search.rows_returned metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_vector_search_rows_returned_metric_attribute_key
      enabled:
        type: boolean
  postgresql_wal_age_metric_config:
    description: PostgresqlWalAgeMetricConfig provides config for the postgresql.wal.age metric.
    type: object
    properties:
      enabled:
        type: boolean
  postgresql_wal_delay_metric_attribute_key:
    description: PostgresqlWalDelayMetricAttributeKey specifies the key of an attribute for the postgresql.wal.delay metric.
    type: string
  postgresql_wal_delay_metric_config:
    description: PostgresqlWalDelayMetricConfig provides config for the postgresql.wal.delay metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_wal_delay_metric_attribute_key
      enabled:
        type: boolean
  postgresql_wal_lag_metric_attribute_key:
    description: PostgresqlWalLagMetricAttributeKey specifies the key of an attribute for the postgresql.wal.lag metric.
    type: string
  postgresql_wal_lag_metric_config:
    description: PostgresqlWalLagMetricConfig provides config for the postgresql.wal.lag metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: postgresql_wal_lag_metric_attribute_key
      enabled:
        type: boolean
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for nrpostgresql resource attributes.
    type: object
    properties:
      postgresql.database.name:
        $ref: postgresql_database_name_resource_attribute_config
      postgresql.index.name:
        $ref: postgresql_index_name_resource_attribute_config
      postgresql.schema.name:
        $ref: postgresql_schema_name_resource_attribute_config
      postgresql.table.name:
        $ref: postgresql_table_name_resource_attribute_config
      server.address:
        $ref: server_address_resource_attribute_config
      server.port:
        $ref: server_port_resource_attribute_config
      service.instance.id:
        $ref: service_instance_id_resource_attribute_config
      service.name:
        $ref: service_name_resource_attribute_config
      service.namespace:
        $ref: service_namespace_resource_attribute_config
  server_address_resource_attribute_config:
    description: ServerAddressResourceAttributeConfig provides config for the server.address resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
  server_port_resource_attribute_config:
    description: ServerPortResourceAttributeConfig provides config for the server.port resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: integer
        x-customType: int64
  service_instance_id_resource_attribute_config:
    description: ServiceInstanceIDResourceAttributeConfig provides config for the service.instance.id resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
  service_name_resource_attribute_config:
    description: ServiceNameResourceAttributeConfig provides config for the service.name resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
  service_namespace_resource_attribute_config:
    description: ServiceNamespaceResourceAttributeConfig provides config for the service.namespace resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
