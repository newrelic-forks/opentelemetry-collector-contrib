change_type: enhancement
component: receiver/oracledb
note: Add SQL comment extraction support for APM correlation. Users can now configure `allowed_comment_keys` to extract key-value pairs from leading SQL block comments and include them as telemetry attributes (`db.query.comment_tags` and `db.query.comment_tags.nr_service_guid`) for correlation with APM traces.
issues: [48338]
change_logs: [user]
